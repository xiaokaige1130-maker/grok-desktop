{
  "name": "linux-grok-desktop",
  "version": "0.6.0",
  "description": "Standalone desktop window for Grok Build CLI — session list, resume, and chat over ACP",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dev": "electron . --enable-logging",
    "smoke": "node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "electron": "^35.0.0"
  }
}
